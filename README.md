# EXCEL-AGGREGATE-FUNCTIONS
 This Excel task covers aggregate functions such as SUM, MIN, MAX, COUNT, and AVERAGE. It demonstrates how these functions can be used to calculate totals, find minimum and maximum values, count data entries, and determine the average of a dataset.
